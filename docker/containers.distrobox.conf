# Distrobox: use project wrappers (set LD_LIBRARY_PATH for host libcrun).
# Paths are absolute so podman-compose child processes resolve them.
[engine]
conmon_path = ["/home/ryuz/Arch/Developer/noite/apps/noite/docker/bin/conmon"]
runtime = "crun"
runtime_path = ["/home/ryuz/Arch/Developer/noite/apps/noite/docker/bin/crun"]
helper_binaries_dir = ["/run/host/usr/lib/podman", "/run/host/usr/lib64/podman"]
