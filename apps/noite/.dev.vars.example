# Local workerd dev secrets (gitignored). Copy to `.dev.vars` and fill in.
# Production values come from `wrangler secret` / deploy env, never this file.
BETTER_AUTH_SECRET=dev-insecure-secret-change-me
BETTER_AUTH_URL=http://localhost:9080
RUNNER_TOKEN=dev-runner-token
RUSTFS_ACCESS_KEY=minio
RUSTFS_SECRET_KEY=minio123
NOITE_ADMIN_EMAIL=
NOITE_EMAIL_WEBHOOK_URL=
